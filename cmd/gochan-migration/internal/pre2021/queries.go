package pre2021

const (
	boardsQuery = `SELECT
	id,
	dir,
	type,
	upload_type,
	title,
	subtitle,
	description,
	section,
	max_file_size,
	max_pages,
	default_style,
	locked,
	anonymous,
	forced_anon,
	max_age,
	autosage_after,
	no_images_after,
	max_message_length,
	embeds_allowed,
	redirect_to_thread,
	require_file,
	enable_catalog
	FROM DBPREFIXboards`

	postsQuery = `SELECT
	id,
	boardid,
	parentid,
	name,
	tripcode,
	email,
	subject,
	message,
	message_raw,
	password,
	filename,
	filename_original,
	file_checksum,
	filesize,
	image_w,
	image_h,
	thumb_w,
	thumb_h,
	ip,
	tag,
	timestamp,
	autosage,
	deleted_timestamp,
	bumped,
	stickied,
	locked,
	reviewed from DBPREFIXposts WHERE deleted_timestamp = NULL`
)
